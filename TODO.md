# TODO

- Make solved-problem tags user-editable. Keep imported Codeforces tags as the initial set, but let users remove individual tags with an inline `x` and persist their overrides.
- Revisit the "Get a new problem" recommender using themeCP's implementation as the reference for next-problem selection.
